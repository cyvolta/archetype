import { PublicWrapper } from '@lib/components';

export default function Hero() {
    return (
        <PublicWrapper id='hero'>
            <div>Hero Section</div>
        </PublicWrapper>
    );
}
